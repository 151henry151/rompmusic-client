# Ngrok authtoken for Expo tunnel (expo start --tunnel).
# Copy to .env.ngrok and add your token. Get one at: https://dashboard.ngrok.com/get-started/your-authtoken
# .env.ngrok is in .gitignore and is not committed.
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
